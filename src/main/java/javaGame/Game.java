package javaGame;

// this is an interface, everything inside it is abstract and public by default
// This is the main way of implementing an API in a program
public interface Game {

    void playGame(User user, ComputerUser computerUser);

}
